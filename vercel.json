{
  "installCommand": "npm install --prefix frontend",
  "buildCommand": "npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/((?!assets/|events/|vite.svg).*)",
      "destination": "/index.html"
    }
  ]
}
